// =========================================
// IMPORTS
// =========================================

import {
    config,
} from './config.js';

// =========================================
// URL
// =========================================

export function appUrl(path = '')
{
    return (
        config.baseUri
        + path
    ).replace(
        /\/{2,}/g,
        '/',
    );
}